# Third-party notices

The Gridy tool runtime redistributes uv under the user's choice of the
Apache License 2.0 or MIT License. The corresponding upstream license texts
are included in the `licenses` directory.

Gridy does not redistribute CPython, Python packages, sim-cli, simparse, or
solver plugins in this runtime. The exact uv version and artifact hash are
recorded in the generated `runtime.json`.
